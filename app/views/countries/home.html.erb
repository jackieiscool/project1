 
  
   <div id="map-canvas"></div>

     <div id="box1" class="animated @keyframes fadeIn "> 
     <h2>Please pick a Place you want to go!</h2> 
     <form action="/countries" method="post">
        <input class = "form-control" type="text" name="place" placeholder="Enter Location"></input>
        <input class="btn btn-lg btn-success" type="submit" value="Submit"></input>
        <br>
        <input type="checkbox"> Been here?</input>
    </form>
    </div>

    

    <div class="box-list2">
      <h2>Been there</h2>
      <% if @countries.blank? %>
        <p>No countries to display</p>
      <% else %>
     
       <% @countries.each do |c| %>
       <p> <%= c.title %> <%= link_to "ELIMINATE", c, method: :delete,:class => "btn btn-sm btn-danger", data:{confirm: "Are you sure it's time to ELIMINATE.." }  %></p><br>
      <% end %>
      
      <% end %>
     </div>
    <p><%=@results%></p>
    <div class="bucket-list">
      <h2>Bucket list</h2>
    </div>
    <!--  Image slider  -->
  <div class="container">
   <div class="row">
    <div class="uploaded-image">
     <div class="col-lg-12 text-center">
      <div id="carousel-example-generic" class="carousel slide">
        <!-- Indicators -->
        <ol class="carousel-indicators hidden-xs">
         <li data-target="#carousel-example-generic" data-slide-to="0" class="active"></li>
         <li data-target="#carousel-example-generic" data-slide-to="1"></li>
         <li data-target="#carousel-example-generic" data-slide-to="2"></li>
        </ol>
        <!-- Wrapper for slides -->
      <div class="carousel-inner">
       <div class="item active">
        <img class="img-responsive img-full"<%= image_tag @country.image.url(:square) %> 
       </div>
       <!-- image -->
      <div class="item">
       <img class="img-responsive img-full" src="http://gamesdbase.com/Media/SYSTEM/Sega_Nomad/Title/big/Captain_Planet_and_the_Planeteers_-_1992_-_Sega.jpg" alt="">
      </div>
        <!-- image -->
      <div class="item">
       <img class="img-responsive img-full" src="http://fc00.deviantart.net/fs71/f/2012/022/1/7/the_avengers_by_tyrannus-d4n93q4.jpg" alt="">
      </div>
     </div>

    <!-- Controls -->
    <a class="left carousel-control" href="#carousel-example-generic" data-slide="prev">
     <span class="icon-prev"></span>
    </a>

    <a class="right carousel-control" href="#carousel-example-generic" data-slide="next">
     <span class="icon-next"></span>
    </a>
   </div>            
   <!--   <hr class="tagline-divider"> -->
  </div>
 </div> 
</div>
<!-- for the image slider -->
<script src="js/jquery-1.10.2.js"></script>
<script src="js/bootstrap.js"></script>


    <script>

    // Activates the Carousel
    $('.carousel').carousel({
        interval: 2000
    })
    </script>
 <script>
  $(function(){ 
    var japan = new google.maps.LatLng(gon.lat, gon.lon);
    var infowindow;
    var listenForInfoWindow = function(contentString, marker){
      google.maps.event.addListener(marker, 'click', function() {
         if(!!infowindow){
                infowindow.close()
          }
          contentString= "<div><p>" + contentString +"</p></div>"
          infowindow = new google.maps.InfoWindow({
          content: contentString
        });

        infowindow.open(map, this);
      });
    };
    function initialize() {
     var mapOptions = {
      zoom: 2,
      center: japan
     };
     map = new google.maps.Map(document.getElementById('map-canvas'),
      mapOptions);
      
      for(
        var i = 0; i < gon.countries.length; i++){
        var loc = new google.maps.LatLng(gon.countries[i].latitude, gon.countries[i] .longitude );

        var marker = new google.maps.Marker({
         map:map,
         draggable:false,
         animation: google.maps.Animation.DROP,
         position: loc,
         icon: 'http://mapicons.nicolasmollet.com/wp-content/uploads/mapicons/shape-default/color-942222/shapecolor-no/shadow-0/border-no/symbolstyle-color/symbolshadowstyle-white/gradient-no/dragon.png'
        });
          
        listenForInfoWindow(gon.countries[i].title, marker);
         }
        }
       google.maps.event.addDomListener(window, 'load', initialize);
   });
</script>



